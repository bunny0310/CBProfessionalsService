task copyAppProduction(type: Copy, dependsOn: build) {
    from("${buildDir}/libs") {
        include "*-all.jar"
        rename { "ProfessionalsService.jar" }
    }
    from(fileTree("${projectDir}/config/").files) {
        include "**/*_prod.yml"
    }
    into("${rootDir}/docker-production")
}